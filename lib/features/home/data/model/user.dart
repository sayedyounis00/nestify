class UserModel {
  String createdAt, email, firstName, lastName, phone, userId;
  UserModel({
    required this.firstName,
    required this.lastName,
    required this.phone,
    required this.email,
    required this.userId,
    required this.createdAt,
  });
}
